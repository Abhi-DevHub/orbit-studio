{
  "name": "@orbit/agents",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "node -e process.exit(0)",
    "typecheck": "tsc --noEmit",
    "test": "node -e process.exit(0)"
  },
  "dependencies": {
    "@orbit/prompts": "workspace:*",
    "@orbit/config": "workspace:*",
    "zod": "^3.23.0",
    "nanoid": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
